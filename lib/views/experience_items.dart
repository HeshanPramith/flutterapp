import 'package:flutter/material.dart';

class TimelineCard {
  String title;
  String description;
  IconData icon;
  String image;

  TimelineCard(this.title, this.description, this.icon, this.image);
}
